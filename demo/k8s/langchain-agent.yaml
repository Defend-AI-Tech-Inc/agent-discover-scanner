apiVersion: apps/v1
kind: Deployment
metadata:
  name: langchain-agent
  namespace: default
  labels:
    app: langchain-agent
    defendai: monitored
spec:
  replicas: 1
  selector:
    matchLabels:
      app: langchain-agent
  template:
    metadata:
      labels:
        app: langchain-agent
    spec:
      containers:
      - name: langchain-agent
        image: python:3.12-slim
        command: ["python3", "/scripts/langchain-agent.py"]
        volumeMounts:
        - name: scripts
          mountPath: /scripts
      volumes:
      - name: scripts
        configMap:
          name: agent-scripts

